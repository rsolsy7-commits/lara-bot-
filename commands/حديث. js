module.exports = {
  config: {
    name: "حديث",
    aliases: ["hadith", "احاديث"],
    version: "1.0",
    author: "روريتا",
    prefix: true,
    category: "ISLAM",
    description: "حديث نبوي شريف",
    guide: "{pn}"
  },

  onStart: async function({ api, event }) {
    const hadiths = [
      "قال رسول الله ﷺ: «تبسمك في وجه أخيك صدقة»",
      "قال رسول الله ﷺ: «إن الله جميل يحب الجمال»",
      "قال رسول الله ﷺ: «خيركم من تعلم القرآن وعلمه»"
    ];
    const pick = hadiths[Math.floor(Math.random() * hadiths.length)];
    api.sendMessage("📜 حديث اليوم:\n" + pick, event.threadID);
  }
};
