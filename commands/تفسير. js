module.exports = {
  config: {
    name: "تفسير",
    aliases: ["tafsir"],
    version: "1.0",
    author: "روريتا",
    prefix: true,
    category: "ISLAM",
    description: "تفسير آية قصيرة",
    guide: "{pn} [نص الآية]"
  },

  onStart: async function({ api, event, args }) {
    if (!args[0]) return api.sendMessage("اكتبي آية للتفسير.", event.threadID);

    const text = args.join(" ");
    api.sendMessage(`📘 تفسير مبسط للآية:\n${text}\n\n✨ (تفسير مختصر)`, event.threadID);
  }
};
