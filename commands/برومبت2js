module.exports = {
  config: {
    name: "برومبت2",
    aliases: ["prompt2"],
    version: "1.0",
    author: "روريتا",
    prefix: true,
    category: "OTHERS",
    description: "مولد برومبت متقدم",
    guide: "{pn} فكرة"
  },

  onStart: async function({ api, event, args }) {
    if (!args[0]) return api.sendMessage("اكتبي الفكرة.", event.threadID);

    const idea = args.join(" ");
    api.sendMessage(`✨ برومبت احترافي:\n${idea} — cinematic lighting, masterpiece`, event.threadID);
  }
};
